---
name: Build Bazzite DX
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    paths-ignore:
      - "**/README.md"
  repository_dispatch:
    types:
      - build
  workflow_dispatch:
  merge_group:

env:
  IMAGE_NAME: "${{ github.event.repository.name }}" # the name of the image produced by this build, matches repo names
  IMAGE_DESC: "${{ github.event.repository.description }}"
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}" # do not edit
  ARTIFACTHUB_LOGO_URL: "https://avatars.githubusercontent.com/u/187439889?s=200&v=4"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ inputs.brand_name}}-${{ inputs.stream_name }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare Matrix
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - id: set-matrix
        run: |
          # Use yq to generate a JSON array of objects from image-versions.yaml
          # This drives the Smart Matrix, containing name and tested status
          MATRIX=$(yq -o=json -I=0 '.images' image-versions.yaml | tr -d '\n')
          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT

  build_push:
    name: Build and push image
    needs: prepare
    runs-on: ubuntu-24.04

    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJSON(needs.prepare.outputs.matrix) }}

    env:
      BASE_IMAGE: ${{ matrix.image.name }}

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      # These stage versions are pinned by https://github.com/renovatebot/renovate
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Show disk space
        run: sudo df -h

      - name: Check if /mnt is there
        id: check_mnt
        run: |
          if sudo mountpoint /mnt; then
            echo "mnt_is_there=1" >>$GITHUB_OUTPUT
          else
            echo "mnt_is_there=0" >>$GITHUB_OUTPUT
          fi

      - name: Free Disk Space (Ubuntu)
        if: ${{ steps.check_mnt.outputs.mnt_is_there == '1' }}
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1

      - name: Mount BTRFS for podman storage
        uses: ublue-os/container-storage-action@main
        if: ${{ steps.check_mnt.outputs.mnt_is_there == '1' }}

      - name: Get current date
        id: date
        run: |
          # This generates a timestamp like what is defined on the ArtifactHub documentation
          # E.G: 2022-02-08T15:38:15Z'
          # https://artifacthub.io/docs/topics/repositories/container-images/
          # https://linux.die.net/man/1/date
          echo "date=$(date -u +%Y\-%m\-%d\T%H\:%M\:%S\Z)" >> $GITHUB_OUTPUT

      - name: Extract base image digest
        id: extract_digest
        shell: bash
        run: |
          # Use yq to extract the image + digest for the current matrix member
          # Fallback to latest tag if not found (though Renovate should keep it synced)
          FULL_IMAGE=$(yq '.images[] | select(.name == "${{ matrix.image.name }}") | .image + "@" + .digest' image-versions.yaml)

          if [[ -z "$FULL_IMAGE" || "$FULL_IMAGE" == "@" ]]; then
            echo "::warning::Digest not found in image-versions.yaml for ${{ matrix.image.name }}. Falling back to ghcr.io/ublue-os/${{ matrix.image.name }}:latest"
            FULL_IMAGE="ghcr.io/ublue-os/${{ matrix.image.name }}:latest"
          fi

          echo "full_image=$FULL_IMAGE" >> $GITHUB_OUTPUT

      # OUTPUTS:
      #   - SOURCE_VERSION: version of the source image. Ex.: testing-41.20250312
      #   - SOURCE_VERSION_MAJOR: major version. Used to identify big releases. Ex.: 41
      - name: Get image major version
        id: fetch_source_meta
        run: |
          set -x
          # SOURCE_VERSION_MAJOR must be a number
          declare -i SOURCE_VERSION_MAJOR=0

          # Inspect the actual matrix-specific pinned image from image-versions.yaml
          SOURCE_VERSION=$(
                skopeo inspect --no-tags --raw --config \
                "docker://${{ steps.extract_digest.outputs.full_image }}" | \
                jq -r '.config.Labels["org.opencontainers.image.version"]'
          )
          if [[ -z $SOURCE_VERSION || "$SOURCE_VERSION" == "null" ]]; then
            echo "::error::SOURCE_VERSION was not fetched correctly"
            exit 1
          fi
          echo "SOURCE_VERSION=$SOURCE_VERSION" >>$GITHUB_OUTPUT

          SOURCE_VERSION_MAJOR=$([[ ${SOURCE_VERSION} =~ ^(.*-)?([[:digit:]]+) ]] && echo "${BASH_REMATCH[-1]}")
          if [[ -z ${SOURCE_VERSION_MAJOR} ]] || (( SOURCE_VERSION_MAJOR <= 0 )); then
            echo "::error::SOURCE_VERSION_MAJOR was not fetched correctly"
            exit 1
          fi

          echo "SOURCE_VERSION_MAJOR=$SOURCE_VERSION_MAJOR" >>$GITHUB_OUTPUT

      - name: Generate output image ref
        id: gen_image_ref
        run: |
          echo "IMAGE_NAME=${BASE_IMAGE/bazzite/bazzite-dx}" | tee -a "$GITHUB_ENV"

      - name: Surface Recipe
        run: |
          set -ex
          # Mirroring Justfile logic: patch the recipe with the actual matrix-specific base image
          mkdir -p .bluebuild
          yq ".base-image = \"${{ steps.extract_digest.outputs.full_image }}\" | .image-version = \"${{ matrix.image.tag }}\"" recipes/recipe.yml > .bluebuild/build-recipe.yml

      - name: Build and Push with BlueBuild
        uses: blue-build/github-action@v1.8
        with:
          recipe: .bluebuild/build-recipe.yml
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ secrets.GITHUB_TOKEN }}
          pr_event_number: ${{ github.event.number }}
          maximize_build_space: true
          # Smart Matrix Tagging
          tag_latest: ${{ matrix.image.tested }}
          tag_stable: ${{ matrix.image.tested }}
          tags: >-
            ${{ matrix.image.tested == false && 'experimental latest-experimental' || '' }}
            ${{ steps.fetch_source_meta.outputs.SOURCE_VERSION_MAJOR }}
            ${{ steps.fetch_source_meta.outputs.SOURCE_VERSION }}
          # Rechunk for better optimization
          rechunk: true


  check:
    name: Check all builds successful
    if: always()
    runs-on: ubuntu-latest
    needs: [build_push]
    steps:
      - name: Check Jobs
        env:
          JOBS: ${{ toJson(needs) }}
        run: |
          echo "Job status:"
          echo $JOBS | jq -r 'to_entries[] | " - \(.key): \(.value.result)"'

          for i in $(echo $JOBS | jq -r 'to_entries[] | .value.result'); do
            if [ "$i" != "success" ] && [ "$i" != "skipped" ]; then
              echo ""
              echo "Status check not okay!"
              exit 1
            fi
          done
